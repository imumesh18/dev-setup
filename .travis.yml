sudo: required

language: bash

services:
  - docker

script:
 - docker run --rm -it --name df-shellcheck -v $(pwd):/usr/src:ro --workdir /usr/src r.j3ss.co/shellcheck ./test.sh

matrix:
fast_finish: true
